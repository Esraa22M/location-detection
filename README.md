# 📍 Live Location Tracker (React Native / Expo)

A simple React Native application built with **Expo**, that requests location permission, displays a live-updating map centered on the user's current GPS position, and shows a moving marker in real time.

---

## 🎯 Features

✅ Requests location permission on app launch  
✅ Handles permission denial gracefully  
✅ Displays a map centered on user's current location  
✅ Shows a marker at user's position  
✅ Marker updates in **real-time** as user moves  
✅ Built using `expo-location` and `react-native-maps`




